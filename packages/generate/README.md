# Generate package

Generate owns work product assembly.

This package will consume approved review artifacts and produce filing-ready output packages.

It should require an explicit approval artifact before producing final output.
