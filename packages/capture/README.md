# Capture package

Capture owns source collection and provenance.

This package will consume external source systems and produce Capture Bundles.

It should not analyze facts or generate work product.
